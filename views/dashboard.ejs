<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            margin: 0;
            padding: 0;
        }
        
        .main-content {
            min-height: calc(100vh - 80px);
            padding: 20px 0;
        }
        .card {
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            border: none;
            border-radius: 15px;
            overflow: hidden;
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
        }
        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.15);
        }
        .card-header {
            background: linear-gradient(45deg, #667eea, #764ba2);
            color: white;
            border: none;
            padding: 20px;
            text-align: center;
        }
        .card-body {
            padding: 25px;
        }
        .btn-custom {
            border-radius: 50px;
            padding: 15px 30px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 1px;
            transition: all 0.3s ease;
            margin: 10px;
            min-width: 200px;
            text-decoration: none;
            display: inline-block;
            text-align: center;
        }
        .btn-primary-custom {
            background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
            border: none;
            color: white;
        }
        .btn-primary-custom:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 20px rgba(79, 172, 254, 0.4);
            color: white;
        }
        .btn-success-custom {
            background: linear-gradient(135deg, #ffd86f 0%, #fc6262 100%);
            border: none;
            color: white;
        }
        .btn-success-custom:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 20px rgba(255, 216, 111, 0.4);
            color: white;
        }
        .btn-info-custom {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border: none;
            color: white;
        }
        .btn-info-custom:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 20px rgba(102, 126, 234, 0.4);
            color: white;
        }
        .welcome-text {
            font-size: 1.2rem;
            color: #6c757d;
            margin-bottom: 2rem;
        }
        .user-info {
            background: rgba(255, 255, 255, 0.1);
            border-radius: 15px;
            padding: 20px;
            margin-bottom: 30px;
            color: white;
            text-align: center;
        }
    </style>
</head>
<body>
    <%- include('./partials/dasboardnav.ejs') %>
    <%- include('./partials/sidebar.ejs') %>

    <div class="container-fluid cj-fluid" style="margin-left:260px; width:calc(100% - 260px)">
        <div class="main-content">
            <div class="container">
                <!-- User Welcome Section -->
                <div class="user-info">
                    <h2><i class="fas fa-user-circle me-2"></i>Welcome back, <%= user.name || user.email || 'User' %>!</h2>
                    <p class="mb-0">Manage your data and explore APIs</p>
                </div>

                <div class="row justify-content-center">
                    <div class="col-md-10 col-lg-8">
                        <div class="card">
                            <div class="card-header">
                                <h1 class="mb-0">🚀 Your Dashboard</h1>
                                <p class="mb-0 mt-2">Quick access to your tools</p>
                            </div>
                            <div class="card-body text-center">
                                <div class="welcome-text">
                                    <p>Choose what you'd like to do:</p>
                                </div>
                                
                                <div class="d-flex flex-column flex-md-row justify-content-center align-items-center flex-wrap">
                                    <a href="/adddata" class="btn btn-custom btn-primary-custom">
                                        <i class="fas fa-plus me-2"></i>
                                        Add New Data
                                    </a>
                                    
                                    <a href="/my/datasets" class="btn btn-custom btn-success-custom">
                                        <i class="fas fa-list me-2"></i>
                                        My Data
                                    </a>
                                    
                                    <a href="/api/users" class="btn btn-custom btn-info-custom">
                                        <i class="fas fa-users me-2"></i>
                                        Users API
                                    </a>
                                    
                                    <a href="/external" class="btn btn-custom btn-info-custom">
                                        <i class="fas fa-globe me-2"></i>
                                        External API
                                    </a>
                                    
                                    <a href="/docs" class="btn btn-custom btn-info-custom">
                                        <i class="fas fa-book me-2"></i>
                                        Documentation
                                    </a>
                                </div>
                                
                                <div class="mt-4">
                                    <small class="text-muted">
                                        • Add New Data: Create new datasets | My Data: Manage existing datasets<br>
                                        • Users API: Local JSON data | External API: Third-party data
                                    </small>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
